# Global npm packages, installed by stage 25 against the nvm-pinned node.
#
# One name per line, '#' comments allowed, same exclusion mechanism as the pacman
# lists (PKG_EXCLUDE_FILE). Created 2026-08-24, when a sweep found six globals
# installed on beast-arch and exactly one of them installed by this script.
#
# NOT listed, and deliberately:
#
#   npm, corepack     bundled with the nvm node. They show up in `npm ls -g`
#                     without anyone having installed them.
#
#   obsidian-headless installed further down stage 25 instead, because it is
#                     gated on OBSIDIAN_VAULT being set -- a machine with no
#                     vault has no use for it. Do not add it here as well.
#
#   @forge/cli        Atlassian Forge CLI. Installed on beast-arch; NOT wanted by
#                     default (Ben, 2026-08-24). Add it per-machine rather than
#                     here if a box turns out to need it.
##@forge/cli

# pi, and its web-search plugin which is useless without it. Ben's call
# 2026-08-24, overriding this file's earlier hold.
#
# Read the hold before removing these, because the reason has not gone away: pi
# moved npm SCOPE from @mariozechner to @earendil-works, which is a supply-chain
# event on the package that holds provider API keys in plaintext auth.json, and
# beast-arch task 39 still records it as UNVETTED. Shipping it to a second
# machine is a deliberate acceptance of that, not an oversight -- and the key
# lands on carbon too, so task 39's verdict now applies to two boxes.
@earendil-works/pi-coding-agent
@ollama/pi-web-search
